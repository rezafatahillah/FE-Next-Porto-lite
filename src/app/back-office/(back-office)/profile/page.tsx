import { ProfileView } from '@/modules/authentication';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Profile',
};

export default function AccountsProfilePage() {
  return <ProfileView />;
}
