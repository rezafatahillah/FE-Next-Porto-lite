import { paths } from '@/utils/routes';

// ----------------------------------------------------------------------

export const PROFILE_BREADCRUMB = [
  { name: 'Dashboard', href: paths.backOffice.root },
  { name: 'Profile', href: paths.backOffice.profile.root },
];
